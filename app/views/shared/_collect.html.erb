<% if @post.collected?(current_user) %>
  <%= link_to 'Uncollect', uncollect_post_path(@post), method: :post, class: "btn btn-warning btn-sm", remote: true %>
<% else %>
  <%= link_to 'Collect', collect_post_path(@post), method: :post, class: "btn btn-primary btn-sm", remote: true %>
<% end %>   